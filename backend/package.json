{
  "name": "multimedia-library-backend",
  "version": "2.0.0",
  "description": "REST API for the personal multimedia library (books, movies, music).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:schema": "psql $DATABASE_URL -f ../database/schema.sql",
    "db:seed": "psql $DATABASE_URL -f ../database/seed.sql"
  },
  "keywords": ["multimedia", "library", "express", "postgres"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0",
    "zod": "^3.23.8"
  }
}
